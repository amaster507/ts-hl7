// TODO: Rename this file to surreal.config.ts and add your credentials

interface SurrealConfig {
  user: string
  pass: string
}

export const Credentials: SurrealConfig = {
  user: 'root',
  pass: 'root',
}
